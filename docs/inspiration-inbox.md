# Inspiration Inbox

References saved for possible use on `akibwa.com`. Public links only — no
private material, no credentials. Each entry records where it came from, why it
was kept, and where it might land, so a link is still useful months later.

When an entry is used, move the detail into the relevant design or architecture
doc and note the outcome here.

## Saved

### 2026-08-15 — @_smontlouis post

- Link: https://x.com/_smontlouis/status/2087976942171529223
- Author: `@_smontlouis` on X
- Kept because: Dan wants to use it somewhere on the site.
- Candidate home: not yet decided.
- Content: not captured. The build environment's egress proxy blocks `x.com`
  and the tweet mirrors, so the post text and media could not be read here.
  Paste the gist into this entry when convenient.

### 2026-08-15 — @dankuntz on the creature.company eye site

- Link: https://creature.company/eyes
- Source: post by Daniel Kuntz (`@dankuntz`) on X, last edited 2026-08-14,
  announcing an "eye experience" site built for `@lilguynet`. Recorded from a
  screenshot Dan supplied; the post URL was not captured and both `x.com` and
  `creature.company` are blocked by the build environment's egress proxy, so
  neither the post nor the site could be read here.
- What it claims: the whole site is 69 KB gzipped — nearly half the size of the
  fonts it embeds — runs at 120fps on Chromium, and is driven by Lark, the same
  engine that runs on the company's hardware. The preview shows pairs of
  animated eyes tracking around a dark field, with a named variant and a rarity
  percentage ("matcha, 1.9% chance") in the corner.
- Kept because: it is the weight argument this site already makes, proved on a
  richer surface. A full interactive scene landing under the cost of its own
  typography is the same trade behind the pre-rendered variant ladder and the
  1.5x DPR cap in `AGENTS.md`.
- Candidate home: a systems or professional-page reference point for performance
  budgets. The eye-tracking treatment itself is a possible model for a live
  surface, but Lark is the company's own engine, so the technique is the
  borrowable part, not the stack.
